import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'call-of-kookmin',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
